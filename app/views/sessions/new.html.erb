<%= form_for :user, url: '/sessions/' do |f| %>
  <table>
    <tr>
      <td>Festival nickname:</td>
      <td><%= f.text_field :user_name %></td>
    </tr>
    <tr>
      <td>Password:</td>
      <td><%= f.password_field :password %></td>
    </tr>
  </table>
  <%= f.submit 'Log in' %>
<% end %>